apiVersion: v1
kind: Service
metadata:
  name: {{ include "diploma-app-chart.fullname" . }}
  labels:
    {{- include "diploma-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diploma-app-chart.selectorLabels" . | nindent 4 }}
